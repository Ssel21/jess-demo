public with sharing class Logger {
    public static void logError(String message) {
        // Implement logging to a custom object or external system
        System.debug('Log Error: ' + message);
    }
}